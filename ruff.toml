exclude = ["neat_ml/sam2", "venv"]

[lint]
# ruff==0.16.0 introduces several default rules breaking linting
# select "old" rules to maintain previous set (see issue #49)
select = ["E4", "E7", "E9", "F"] 
extend-select = ["B006", "B008", "UP032"]
